spring:
  application:
    name: auth-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ***
    username: ***
    password: ***
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
eureka:
  instance:
    hostname: localhost